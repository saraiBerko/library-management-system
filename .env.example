# Copy to .env and adjust as needed. docker-compose.yml sets sane defaults
# for local development; this file is mainly a reference for running
# services outside of Docker Compose.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/library
CORS_ORIGINS=["http://localhost:5173"]
VITE_API_URL=http://localhost:8000
